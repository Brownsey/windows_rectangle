# Rectangle Upstream Snapshot

Source repository: <https://github.com/rxhanson/Rectangle>

Snapshot commit: `6cfcb4720b3a6f83df82a8896a3da4751e90ca4e`

Snapshot date: `2026-08-01`

Upstream commit date: `2026-07-28 22:12:35 -0400`

Upstream commit subject:

```text
Prevent Todo shortcuts from firing during shortcut recording (#1792)
```

Snapshot method:

```bash
git clone --depth 1 https://github.com/rxhanson/Rectangle.git
```

All upstream files were copied into `apps/mac/Rectangle` except the upstream
`.git` directory.

The copied source should remain unchanged when parity with upstream Rectangle is
required. Add local packaging, signing, compliance, or release process files in
`apps/mac` rather than inside `apps/mac/Rectangle`.
